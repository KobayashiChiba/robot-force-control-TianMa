# 2026-07-09 工作日报 — 力分解与运动趋势

**项目**：机器人末端力控 | **日期**：2026-07-09

---

## 一、今日完成

### 1. 力分解模块（force_mechanics.py）核心算法

实现了基于非正交基 {t, n, v} 的力分解框架，包含以下核心函数：

**`compute_point_basis(P, geom)`** — 计算交线曲线上任意点的非正交基底：
- **t（切向）**：沿曲线运动方向，解析公式计算
- **n（法向/打磨方向）**：两圆柱面径向法向的角平分线，指向打磨深度方向
- **v（垂直/Z 柱面法向）**：圆柱 2（Z 方向圆柱）的表面径向法向量

内部自动定位点所属分支和参数 t，无需调用者手动指定。

**`decompose_force(F, basis)`** — 外力在 {t, n, v} 上的严格分解：
- 解 3×3 线性方程组 M·coeffs = F，而非简单的点积投影
- 重建误差 < 1e-14（仅受浮点精度限制）
- 返回 `ForceDecomp` 数据类：包含各分量向量和分解系数

**运动趋势计算**：
- `compute_normal_motion_trend()` — 法向力引起的运动趋势（方向 = n，大小含基线偏移 offset=8.0）
- `compute_vertical_motion_trend()` — 垂直力引起的运动趋势（方向 = -(t×v)，大小为 |垂直系数|）

### 2. 两种力分解方案对比

| 对比维度 | 方案1：Frenet 正交分解 | 方案2：{t, n, v} 分解（当前） |
|----------|----------------------|------------------------------|
| 基底 | t, n_curve, bi-normal (n×t) | t, n_grinding, v (Z柱面法向) |
| bi-normal 物理解释 | 无对应物理量 | — |
| v 物理解释 | — | Z 圆柱壁接触力 |
| 适用性 | 纯几何曲线分析 | 打磨力控任务 |

![方案对比](C:/Users/KCserver/projects/formal/机器人末端力控/code/output/fig_schemes_compare.png)

### 3. 力分解示意图与运动趋势

![力分析](C:/Users/KCserver/projects/formal/机器人末端力控/code/output/fig_force_analysis.png)

---

## 二、方案2 的优势分析

当前方案将外力 F 分解为三个具有明确物理意义的力分量：

### 切向力 Ft（a·t）
- **来源**：毛刺阻碍 + 球刀震动
- **处理策略**：**可忽略不计**。打磨过程中切向阻力主要由材料去除产生的摩擦力和刀具振动引起，对打磨质量控制贡献较小，不作为力控调节的主要依据。

### 法向力 Fn（b·n）
- **物理含义**：**球刀切入深度的直接表征**
- **控制策略**：Fn 过大 → 球刀沿法向（n 方向）运动 → 减小切入深度 → Fn 回落至目标范围
- n 为两圆柱面法向的角平分线，指向材料内部，是打磨去毛刺的主要受力方向

### 垂直力 Fv（c·v）
- **物理含义**：**球刀是否接触到 Z 轴圆柱壁**
- **控制策略**：Fv 过大 → 球刀向马鞍面内部移动（远离 Z 圆柱壁）→ 避免过切
- v 为 Z 方向圆柱的表面法向，垂直于该圆柱面，可检测刀具是否偏离了打磨路径（意外接触圆柱壁面）

### 为什么不用方案1（Frenet 正交分解）？

Frenet 框架的 bi-normal（复法向 = n×t）在打磨场景中**没有物理意义**——它既不指向圆柱面法向，也不指向材料内部方向。正交分解的数学便利性并不转化为力控可用性。当前方案用 v（Z 圆柱面法向）替代 bi-normal，使三个力分量各自对应一个可操作的物理量。

---

## 三、下一步

- 基于力分解结果设计力控策略（法向力 PID + 垂直力阈值检测）
- 仿真验证：在包含误差的模拟曲线上运行力控算法
