# 正交圆柱相交曲线分析 & 打磨法向量计算

## 1. 概述

本项目用 Python + matplotlib 对**两个正交圆柱的相交曲线**进行三维可视化分析，并计算曲线上每点的打磨法向量（指向金属区域）。适用于机械加工中圆柱孔相交边缘的打磨路径规划。

- **核心文件**: `test.py`
- **输出图像**: `intersection_curve.png`
- **依赖**: `numpy`, `matplotlib`

---

## 2. 数学模型

### 2.1 圆柱方程

设有两个圆柱，轴线分别平行于坐标轴，轴线方向不同（正交）：

| 参数 | 圆柱 1 | 圆柱 2 |
|---|---|---|
| 轴线方向 | `AXIS1` | `AXIS2` |
| 轴线上一点 | \((C_{x1}, C_{y1}, C_{z1})\) | \((C_{x2}, C_{y2}, C_{z2})\) |
| 半径 | \(R_1\) | \(R_2\) |

圆柱面的隐式方程（以轴线沿 X 为例）：\((y - C_y)^2 + (z - C_z)^2 = R^2\)

### 2.2 交线求解（通用正交情形）

对于轴线方向不同的两个圆柱，存在一个**公共径向坐标**（即同时作为两个圆柱径向的坐标轴）。设：

- `common` → 公共径向坐标索引
- `other1` → 圆柱1的另一径向 = 圆柱2的轴向索引
- `other2` → 圆柱2的另一径向 = 圆柱1的轴向索引

从两个圆柱方程分别解出 `other1` 和 `other2`：

$$\text{other1} = C_1[\text{other1}] \pm \sqrt{R_1^2 - (t - C_1[\text{common}])^2}$$

$$\text{other2} = C_2[\text{other2}] \pm \sqrt{R_2^2 - (t - C_2[\text{common}])^2}$$

其中 \(t\)（公共坐标）的有效范围：

$$t \in \big[\max(C_1[\text{common}]-R_1,\; C_2[\text{common}]-R_2),\;\; \min(C_1[\text{common}]+R_1,\; C_2[\text{common}]+R_2)\big]$$

正负号组合产生 4 条半曲线，在端点 \(t_{\min}\) 或 \(t_{\max}\) 处（根号内为零时）自然汇合，形成 1~2 条闭合空间曲线。

### 2.3 打磨法向量

**物理场景**：圆柱内部 = 空气（被打穿的孔），圆柱外部 = 金属。相交曲线是金属-空气边界上的一条脊线，需沿此曲线打磨倒角。

**计算**：曲线上任一点 \(P\) 同时在两个圆柱面上，各自有向外的单位径向法向量：

$$\vec{n}_1 = \frac{P - C_1 - \big((P-C_1)\cdot\vec{d}_1\big)\vec{d}_1}{R_1}$$

$$\vec{n}_2 = \frac{P - C_2 - \big((P-C_2)\cdot\vec{d}_2\big)\vec{d}_2}{R_2}$$

其中 \(\vec{d}_1, \vec{d}_2\) 为两圆柱轴线方向的单位向量。

打磨法向量取二者的角平分线，指向金属（外侧）：

$$\vec{n}_{\text{grind}} = \frac{\vec{n}_1 + \vec{n}_2}{|\vec{n}_1 + \vec{n}_2|}$$

---

## 3. 可配置参数

所有参数集中在 `test.py` 顶部，修改后直接运行即可：

### 圆柱几何参数

| 参数 | 类型 | 含义 |
|---|---|---|
| `AXIS1`, `AXIS2` | `'X'` / `'Y'` / `'Z'` | 圆柱轴线方向（必须不同） |
| `CX1, CY1, CZ1` | float | 圆柱1 轴线上一点 |
| `CX2, CY2, CZ2` | float | 圆柱2 轴线上一点 |
| `R1`, `R2` | float | 圆柱半径 |
| `LEN1`, `LEN2` | float | 圆柱显示长度（不影响交线计算） |

### 显示参数

| 参数 | 类型 | 含义 |
|---|---|---|
| `CYL_ALPHA` | 0~1 | 圆柱面透明度 |
| `AXIS_RANGE_X / _Y / _Z` | (min, max) | 各轴显示范围 |
| `NORMAL_SAMPLE` | int | 每隔 N 个采样点画一个法向量箭头 |
| `NORMAL_LENGTH` | float | 法向量箭头长度 |
| `OUTPUT_FILE` | str | 输出图像文件名 |

---

## 4. 代码架构

```
test.py
├── 可配置参数区          ← 用户只需改这里
├── 工具函数
│   ├── make_cylinder()   ← 生成圆柱面 meshgrid（通用，支持 X/Y/Z 轴向）
│   └── axis_line()       ← 生成轴线端点坐标（通用）
├── 圆柱面生成            ← 调用 make_cylinder()
├── 交线求解（通用）      ← 自动识别公共径向坐标，解出四段半曲线
├── 打磨法向量计算        ← 每点计算 n1, n2, n_grind
└── 绘图 & 保存
    ├── 半透明圆柱面
    ├── 相交曲线（四段，同色）
    ├── 法向量箭头 (quiver)
    └── 轴线虚线
```

### 关键设计决策

1. **通用轴向支持**：`make_cylinder` 和交线求解都基于坐标索引（0=X, 1=Y, 2=Z），通过集合运算自动确定各坐标的角色，无需为每种轴向组合写分支代码。

2. **四段独立绘制**：交线的 4 条半曲线各自独立调用 `ax.plot()`，而非拼成一个数组。这避免了拼接顺序错误导致的虚假连线。

3. **先保存再显示**：`plt.savefig()` 必须在 `plt.show()` 之前调用，因为 `show()` 关闭窗口后会清空图像对象。

---

## 5. 测试过的参数组合

| 场景 | AXIS1 | AXIS2 | R1 | R2 | 交线特征 |
|------|-------|-------|----|----|---------|
| 初始测试 | X | Z | 10 | 20 | 1 条闭合曲线，Y∈[8,10] |
| 验证轴向切换 | Y | Z | 10 | 20 | 1 条闭合曲线 |
| 中心偏移 | Y | Z | 10 | 20 | 验证法向量方向 |

所有场景圆柱2中心偏移均为 (0, 28, 0) 或 (27, 0, 0)。

---

## 6. 踩坑记录

| 问题 | 原因 | 解决 |
|---|---|---|
| 相交曲线出现多余横/竖连线 | 四段拼接顺序不对，不相邻的段首尾被 matplotlib 连起来 | 改为四段各自独立 `ax.plot()` |
| 保存的图像为空白 | `plt.show()` 关闭后图像对象被清空，`savefig` 写了空图 | `savefig` 移到 `show` 之前 |
| `set_box_aspect([1,1,1])` 不等于三轴等比例 | box aspect 只让包围盒为立方体，数据跨度不同时比例仍不一致 | 手动设三轴 `set_xlim/ylim/zlim` 为相同跨度 |
| 轴线方向硬编码导致无法复用 | 圆柱面生成和交线求解都假设固定轴向 | 抽象为基于坐标索引的通用函数 |

---

## 7. 运行方式

```bash
cd e:\Code\Claude\ForceFeedback
python test.py
```

运行后弹出交互式 3D 窗口（可拖拽旋转/缩放），关闭窗口后图像自动保存到 `intersection_curve.png`。
